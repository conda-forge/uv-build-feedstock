{% set name = "uv_build" %}
{% set version = "0.8.6" %}

package:
  name: uv-build
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/uv_build-{{ version }}.tar.gz
  sha256: f11895ba05e6389c95ce952923be3b7b70953a83b9c3670f0d082f45df2a1a0f

build:
  number: 0

requirements:
  build:
    - {{ compiler('c') }}
    - {{ stdlib('c') }}
    - {{ compiler('rust') }}
    - cargo-bundle-licenses
    - python                                        # [build_platform != target_platform]
    - maturin >=1.0,<2.0                            # [build_platform != target_platform]
    - cross-python_{{ target_platform }}            # [build_platform != target_platform]
  host:
    - python
    - maturin >=1.0,<2.0
    - pip
  run:
    - python

test:
  imports:
    - uv_build
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://pypi.org/project/uv/
  summary: The uv build backend
  license: Apache-2.0 AND MIT AND BSD-2-Clause
  license_file:
    - LICENSE-MIT
    - LICENSE-APACHE
    - crates/uv-pep440/License-Apache
    - crates/uv-pep440/License-BSD
    - crates/uv-build/LICENSE-MIT
    - crates/uv-build/LICENSE-APACHE
    - crates/uv-pep508/License-Apache
    - crates/uv-pep508/License-BSD
    - THIRDPARTY.yml

extra:
  recipe-maintainers:
    - isuruf
